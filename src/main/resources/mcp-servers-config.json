{
  "mcpServers": {
    "filesystem": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "--mount", "type=bind,src=/tmp,dst=/projects/Desktop",
        "mcp/filesystem",
        "/projects"
      ]
    }
  }
}